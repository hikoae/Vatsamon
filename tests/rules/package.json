{
  "name": "vatsamon-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test suite per firestore.rules (Vatsamon GO) via Firebase Emulator + @firebase/rules-unit-testing",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project vatsamon-rules-test \"node --test rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
